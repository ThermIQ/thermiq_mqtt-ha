# ############################################################################
# Automations for ThermIQ -> automation.yaml
# ### ThermIQ, From Input number -> MQTT
  - alias: ThermIQ, Input number to MQTT
    trigger:
      platform: state
      entity_id: 
#input_number entity_ids for ThermIQ
        - input_number.thermiq_indoor_requested_t
        - input_number.thermiq_integral1_curve_slope
        - input_number.thermiq_integral1_curve_min
        - input_number.thermiq_integral1_curve_max
        - input_number.thermiq_integral1_curve_p5
        - input_number.thermiq_integral1_curve_0
        - input_number.thermiq_integral1_curve_n5
        - input_number.thermiq_heating_stop_t
        - input_number.thermiq_reduction_t
        - input_number.thermiq_room_factor
        - input_number.thermiq_integral2_curve_slope
        - input_number.thermiq_integral2_curve_min
        - input_number.thermiq_integral2_curve_max
        - input_number.thermiq_integral2_curve_target
        - input_number.thermiq_integral2_curve_actual
        - input_number.thermiq_outdoor_stop_t
        - input_number.thermiq_preassure_pipe_limit_t
        - input_number.thermiq_hotwater_start_t
        - input_number.thermiq_hotwater_runtime_m
        - input_number.thermiq_heatpump_runtime_m
        - input_number.thermiq_legionella_runtime_m
        - input_number.thermiq_legionella_stop_t
        - input_number.thermiq_integral1_a_limit
        - input_number.thermiq_integral1_hysteresis_t
        - input_number.thermiq_returnline_max_t
        - input_number.thermiq_start_interval_min_m
        - input_number.thermiq_brine_min_t
        - input_number.thermiq_cooling_target_t
        - input_number.thermiq_integral2_a_limit
        - input_number.thermiq_integral2_hysteresis_t
        - input_number.thermiq_elect_boiler_steps_max
        - input_number.thermiq_current_consumption_max_a
        - input_number.thermiq_shunt_time_s
        - input_number.thermiq_hotwater_stop_t
# ######    #condition: 
      #condition: template
      # This condition does not work... Temporary fix in write_id
      # value_template: "{{ ( (states(trigger.to_state.state ) | int) != (states('input_number.' + trigger.to_state.object_id) | int) ) }}"
    action: 
    # Send an MQTT message
      - service: thermiq_mqtt.write_id
        data_template:
          value_id: "{{ trigger.entity_id  }}"
          value: '{{ trigger.to_state.state | int }}'
          bitmask: 0xffff

# ############################################################################
# ### ThermIQ, From State to -> Input number        
# ###
  - alias: ThermIQ, thermiq_mqtt.indoor_requested_t to input_number.thermiq_indoor_requested_t
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.indoor_requested_t
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_indoor_requested_t 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.integral1_curve_slope to input_number.thermiq_integral1_curve_slope
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.integral1_curve_slope
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_integral1_curve_slope 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.integral1_curve_min to input_number.thermiq_integral1_curve_min
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.integral1_curve_min
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_integral1_curve_min 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.integral1_curve_max to input_number.thermiq_integral1_curve_max
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.integral1_curve_max
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_integral1_curve_max 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.integral1_curve_p5 to input_number.thermiq_integral1_curve_p5
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.integral1_curve_p5
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_integral1_curve_p5 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.integral1_curve_0 to input_number.thermiq_integral1_curve_0
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.integral1_curve_0
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_integral1_curve_0 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.integral1_curve_n5 to input_number.thermiq_integral1_curve_n5
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.integral1_curve_n5
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_integral1_curve_n5 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.heating_stop_t to input_number.thermiq_heating_stop_t
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.heating_stop_t
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_heating_stop_t 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.reduction_t to input_number.thermiq_reduction_t
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.reduction_t
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_reduction_t 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.room_factor to input_number.thermiq_room_factor
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.room_factor
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_room_factor 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.integral2_curve_slope to input_number.thermiq_integral2_curve_slope
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.integral2_curve_slope
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_integral2_curve_slope 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.integral2_curve_min to input_number.thermiq_integral2_curve_min
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.integral2_curve_min
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_integral2_curve_min 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.integral2_curve_max to input_number.thermiq_integral2_curve_max
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.integral2_curve_max
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_integral2_curve_max 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.integral2_curve_target to input_number.thermiq_integral2_curve_target
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.integral2_curve_target
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_integral2_curve_target 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.integral2_curve_actual to input_number.thermiq_integral2_curve_actual
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.integral2_curve_actual
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_integral2_curve_actual 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.outdoor_stop_t to input_number.thermiq_outdoor_stop_t
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.outdoor_stop_t
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_outdoor_stop_t 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.preassure_pipe_limit_t to input_number.thermiq_preassure_pipe_limit_t
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.preassure_pipe_limit_t
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_preassure_pipe_limit_t 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.hotwater_start_t to input_number.thermiq_hotwater_start_t
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.hotwater_start_t
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_hotwater_start_t 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.hotwater_runtime_m to input_number.thermiq_hotwater_runtime_m
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.hotwater_runtime_m
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_hotwater_runtime_m 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.heatpump_runtime_m to input_number.thermiq_heatpump_runtime_m
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.heatpump_runtime_m
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_heatpump_runtime_m 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.legionella_runtime_m to input_number.thermiq_legionella_runtime_m
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.legionella_runtime_m
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_legionella_runtime_m 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.legionella_stop_t to input_number.thermiq_legionella_stop_t
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.legionella_stop_t
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_legionella_stop_t 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.integral1_a_limit to input_number.thermiq_integral1_a_limit
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.integral1_a_limit
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_integral1_a_limit 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.integral1_hysteresis_t to input_number.thermiq_integral1_hysteresis_t
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.integral1_hysteresis_t
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_integral1_hysteresis_t 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.returnline_max_t to input_number.thermiq_returnline_max_t
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.returnline_max_t
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_returnline_max_t 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.start_interval_min_m to input_number.thermiq_start_interval_min_m
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.start_interval_min_m
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_start_interval_min_m 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.brine_min_t to input_number.thermiq_brine_min_t
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.brine_min_t
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_brine_min_t 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.cooling_target_t to input_number.thermiq_cooling_target_t
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.cooling_target_t
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_cooling_target_t 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.integral2_a_limit to input_number.thermiq_integral2_a_limit
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.integral2_a_limit
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_integral2_a_limit 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.integral2_hysteresis_t to input_number.thermiq_integral2_hysteresis_t
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.integral2_hysteresis_t
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_integral2_hysteresis_t 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.elect_boiler_steps_max to input_number.thermiq_elect_boiler_steps_max
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.elect_boiler_steps_max
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_elect_boiler_steps_max 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.current_consumption_max_a to input_number.thermiq_current_consumption_max_a
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.current_consumption_max_a
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_current_consumption_max_a 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.shunt_time_s to input_number.thermiq_shunt_time_s
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.shunt_time_s
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_shunt_time_s 
        value: "{{ trigger.to_state.state | int }}"

        
# ###
  - alias: ThermIQ, thermiq_mqtt.hotwater_stop_t to input_number.thermiq_hotwater_stop_t
    trigger:
      platform: state
      entity_id:
        - thermiq_mqtt.hotwater_stop_t
    action:
    - service: input_number.set_value
      data_template:
        entity_id: input_number.thermiq_hotwater_stop_t 
        value: "{{ trigger.to_state.state | int }}"


# # ######
 
# ############################################################################
# ### ThermIQ, From Input select -> MQTT
  
  - alias: ThermIQ, Inputs select to MQTT
    trigger:
      platform: state
      entity_id: 
        - input_select.thermiq_main_mode
    action: 
    # Send an MQTT message
      - service: thermiq_mqtt.write_id
        data_template:
          value_id: "{{ trigger.entity_id  }}"
          value: "{{ state_attr(trigger.entity_id,'options').index(states(trigger.entity_id)) | int }}"
          bitmask: 0xffff




# ############################################################################
# ### ThermIQ, From Sensor to -> Input select

  - alias: ThermIQ, Sensor to Inputs Select
    trigger:
      platform: state
      entity_id: 
        - thermiq_mqtt.main_mode
    condition:
      condition: template
      value_template: "{{ ( ((trigger.to_state.state | int )  >=0 ) and ((trigger.to_state.state | int )  <5 ) ) }}"
    action: 
      service: input_select.select_option
      data_template:
        entity_id: 'input_select.thermiq_main_mode'
        option: "{{ state_attr('input_select.thermiq_main_mode','options')[(trigger.to_state.state | int )] }}"
# ############################################################################
  
